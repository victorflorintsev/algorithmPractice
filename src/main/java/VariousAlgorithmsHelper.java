public class VariousAlgorithmsHelper {
    /**
     * Inserts an element at the correct position into an array efficiently, resizing if need be.
     * @param element
     * @param array
     * @param <T>
     */
    public <T extends Comparable<? super T>> void insertIntoSortedArray(T element, T[] array) {

    }
}
